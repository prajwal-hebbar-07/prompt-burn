{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Dashboard window. Frontend IPC is the app's own sidecar_request command, which needs no plugin permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}