{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Static shell window. No frontend IPC yet.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
